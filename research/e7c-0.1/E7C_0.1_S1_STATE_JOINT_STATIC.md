# S1 candidate: State and Joint static successor

Status: `draft; exact_head_review_required; static_fragment_only`

Base: main `e431946d6499ecdbc2028702f495980c9280d641`
Edition: `E7C-S1-STATE-JOINT/0.1-provisional`
Registered module: `E7C-EECQ-FG3-BRIDGE/0.1-provisional`, signature `FG3-graph`.
Published reference: v0.12.1/RGP2, unchanged.

The previously merged typed FG3 bridge checks examples in a profile module.
This S1 candidate begins the separate central type grammar. Its `State[module,
signature]` is a finite formal state type; `Joint[State₁,...,Stateₙ]` retains
coordinate order and a single registered module edition, with arity 2–8.
Structural equality retains the module, signature, coordinate types and order.
No coercion turns a joint into a state. Admission rejects unknown module and
signature pairs, malformed coordinates, invalid arity and nested outcomes.

The closed term fragment is variable lookup, a binary independent product of
two states of the same registered module, and an indexed joint marginal.
Static resource effects record finite product and marginal operations. The
checker validates a bounded JSON research AST. It does **not** evaluate
coefficients or values, establish that a particular profile implements the
declared product, define an exact resource budget, handle a strict joint
operation, provide replay, or prove type preservation. The current accepted
B1 parser and evaluator remain on their original edition and syntax; this
file does not silently expand them.

The next gate is to bind these static rules to exact FG3 finite values, with
an operational rule and a separate replay check for product and marginal;
then review preservation and reflection against the pinned source model.
Quote, phase, strict joins, other profiles and WPC follow under separate
admission. No product, API, data, ID or canonical source migration occurs.
