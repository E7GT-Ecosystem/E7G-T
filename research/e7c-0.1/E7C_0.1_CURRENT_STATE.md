# E7C/0.1 current state

Direction: `E7-ECO-DIR-2026-09-15.1`

Last reconciled baseline: main `0f6840eb16fc9bad2ae53e7b7056bb9d87063b04` (PR #117 merge; bounded first-party evidence, not general semantic acceptance). PRs #100–#111 include merged provisional correlated Joint slices and gates; #112 pins the RGP successor delta, #113 adds a finite read-only expression candidate, #114 binds finite SF/CFS replay to the draft editions, #115 separates WPC/0.2 semantic constitution from SR4 encoding, and #116 proves one pinned MSC Boolean obstruction in Lean with a checked finite fixture encoding, and #117 merges the bounded REC evidence-only rule. The all-input Python source/IR correspondence to the staged Lean evaluator remains open. Full calculus completion additionally requires the component and edition gates in `E7C_0.1_EDITION_COMPLETION_GATE.md`. A component with an unproved required link is covered with an open link, not complete.

REC merged bounded slice (PR #117): a separately implemented, one-claim
evidence-only status rule with source-admitted input and no rule closure. Its
source-to-rule proof link remains open; this does not prove general REC
fidelity or close the v0.14 completion row.

Next Joint actual-code proof review candidate: the source and IR all-input
theorems are stated separately in `E7C_0.1_JOINT_ACTUAL_CODE_PROOF_GATE.md`.
An admitted source document can exceed the nested IR package size limit,
so IR lowering needs an explicit premise or a narrowed admission edition.
Exact AST pins and first-stage path regressions are bounded code-identity
evidence; a verified translation of running Python/IR control flow remains
open. This package does not discharge the implementation proof.

Historical WP5 base: `64758c6cd9b177b196f2cf786ee88253626b7dca`

Historical WP0 source cutoff: `2026-09-15T19:52:58Z`

Status: `wp0_wp1_wp2_wp3s_accepted_for_bounded_scope; wp3i_integrated_without_independent_adequacy; wp4_lean_bounded; wp5_profile_slices_merged_internal_partial; wp6_selected_ir_internal_partial; b2_b3_opt_in_provisional; no_general_adequacy_or_profile_promotion`

## Completed in this bounded work package

- immutable canonical and successor source registry;
- repository PR/issue/path enumeration through cutoff, including an exactly-once
  frozen ledger for every PR number from #1 through #33;
- all 18 gap-register entries dispositioned;
- explicit exclusions and visibility limits;
- component-level retain/adapt decisions with preservation, loss and
  ID/data/API consequences;
- terminology/anti-collapse and compatibility matrices;
- open architecture decisions and risk controls;
- bounded, unfrozen WP1 charter and typed formal vocabulary;
- equality ladder and conversion prohibition;
- profile/module and adapter-fidelity contract;
- WP1 formal-review matrix.

WP1 was accepted at PR #35 head `b1c8ca7…` and merged as `1f4c7cf…`.
WP2 was accepted at PR #36 head `e0d73ed8…` and merged as `ed2a49de…`.
WP3-S was accepted at PR #37 head `bae84fb73e…` and merged as `c13d77dc5b…`.
The witness contract was accepted at PR #38 head `eea2c88290ee…` and merged as
`a6c562108450…`.

## Accepted bounded WP2 work

- finite internal JSON AST for ten type forms and six term forms;
- exact nominal declaration resolution;
- structural B1 type conversion only;
- finite set-union effect rows for the implemented straight-line fragment;
- typed total/strict/filtering map-policy distinction;
- layered admission and controlled static diagnostics;
- dependency-free static checker with positive and negative fixtures.

## Accepted bounded WP3-S work

- specification-only deterministic big-step evaluation relation;
- disjoint operational outcome boundary with admission diagnostics outside it;
- explicit resource policy, progress and exhaustion distinctions;
- ordered runtime ledger with WP2 effect-row erasure correspondence;
- operational rules for every accepted WP2 constructor;
- model-relative denotations and an operational–denotational matrix;
- observable behaviour including outcome, ledger and resource information;
- required counterexamples and a separate semantic review gate.

WP3-GAP-001 formerly recorded ambiguity around outcome-typed variables and
consumed outcome positions. The revised WP3-S candidate applies the bounded
outcome-normal static amendment: outcome variables return their terminal
binding directly, recursive value-position nesting and consumption (including
restriction over `Family[I,Outcome[A,xi]]`) are rejected with `E7C-S013`, and no
eliminator or implicit flattening is introduced. This resolution was accepted
for the bounded specification.

## Accepted witness-contract work

- canonical witness envelope and derivation-record requirements;
- independent replay from complete pinned inputs rather than evaluator state;
- exact digest, edition, identity, resource, outcome and ledger checks;
- explicit treatment of redacted or hash-only material as non-replayable;
- claim separation between bounded derivation replay and source truth, domain
  authority or external-model correctness;
- tamper, mismatch and authority-escalation rejection cases;
- WP3-I entry gate and mandatory first evaluator/checker tests.

## Integrated WP3-I candidate

- disposable evaluator over the accepted unary B1 term forms;
- separately implemented replay checker that does not import evaluator control
  flow;
- shared canonical-data and admission layers limited to encoding, digests,
  identity binding and closed finite-package validation, with no dynamic rule
  control flow;
- direct and nested outcome propagation, deterministic charge precedence and
  ordered ledger preservation;
- exhaustive canonical finite-fibre reconstruction with no partial success;
- integrity, missing-material, edition, authority-escalation and semantic replay
  rejection cases.
- nominal view/policy/source-return validation and complete referenced
  interpretation-table validation, including rebound-witness adversarial cases.
- edition-pinned closed carriers and recursively typed runtime bindings;
- candidate charging for restriction, reconstruction and classification, with
  last-key progress advancing only after a completed check;
- derivation graph rejection for duplicate IDs, cycles, orphans and dangling
  children.

Review at head `fcab070145ae…` identified both the initially reported runtime
admissions and a broader material set: incomplete closed-package admission,
missing restriction/classification charges, premature last-key progress and
under-validated derivation graphs. These findings are corrected on the same
draft branch. The corrections were published at `fe7ed454fa28…` and merged by
explicit owner direction as `9f997801eba3…`. No later independent acceptance
verdict was recorded, so integration is not represented as conformance,
correctness or independent reproduction.

## Current WP4-S candidate

- theorem statements and hypotheses for formation, exchange, weakening,
  substitution, structural conversion, determinism, preservation,
  ledger/effect soundness, progress, exact reconstruction and bounded witness
  replay;
- proof sketches separated from bounded executable evidence;
- explicit stated deferrals for composition, quotient descent, adapters,
  lowering and general denotational adequacy;
- a persistent counterexample catalogue covering current executable cases and
  named later-profile obligations;
- a Lean/Rocq/Isabelle comparison and common spike acceptance test, with tool
  selection explicitly deferred because none is installed in the builder
  environment;
- bounded regression checks for repeatability, ledger erasure, finite-resource
  outcomes and exact fixture reconstruction.

Review at head `157ed73ad510…` found unpinned replay identity/policy fields,
canonical-invalid inputs escaping as host exceptions, incomplete per-theorem
records and a conflation between implementation replay equality and formal
semantic soundness. The revision pins both editions before replay, closes the
canonical JSON and external-assumption boundary, rejects Boolean bounds, and
returns controlled admission errors. T001–T017 now each record applicable
hypotheses, an exact statement, status, counterexample search and mechanisation
disposition. T009 separately covers abstract progress and the evaluator API;
T011 separately records implementation replay equality and the still-stated
WP3-S semantic-soundness obligation.

PR #40 was internally accepted at exact head
`edb81b7e1b1a13f67234feb09981bba8a43d2a36` and merged as
`f8748d315bdfc22546d9172c33138ac1a8799f0c`. The editorial CE-026 test-name
correction was isolated in PR #41. The bounded Lean feasibility spike was
accepted at PR #42 exact head `028922f98f3e31f0c9f97ddb0171c3bd83c2f526`
and merged as `2dcd06c6fda9c13b9ce40500bc835912e06cd245`.

## Merged bounded WP4 Lean core extension

- separate Lean package preserving the accepted feasibility spike unchanged;
- variables, strict maps, source-preserving views and restrictions;
- deterministic functional evaluation and matching Lean inductive rules;
- successful preservation under explicit typed, unique and complete runtime
  environment and interpretation premises;
- static effect containment plus ordered-ledger prefix preservation; and
- a correspondence matrix that names every represented external WP2/WP3-S rule
  and every deliberate abstraction.

The package omits resource indexing, charge precedence, total/filtering maps,
projections, reconstruction, classification, obligations, replay and
denotational semantics. Exact-head review at
`01f7087bbe6dfd0022033c442b922df366ad7814` returned
`lean_core_revision_required`: WP2 set effects were conflated with WP3 trace
order, a raw ill-typed nested failure was claimed as typed coverage, and CI
actions used mutable tags. Corrections were prepared on a separate reviewed
head. The three corrections were
accepted for integration at PR #45 head
`9b2c636288150c33a55d2d76348cad5352eb99ca` and merged into PR #43 as
`35ecbc1bfc54376aa48de5b160ab4533c5c7aba4`. Both hosted Lean workflows
passed at the combined head, as did 187 repository tests locally. Final
review at `4dab1a8a12c64f23cf91dfff6b56c566d9dd301f` accepted this bounded
internal package; PR #43 merged as `64758c6cd9b177b196f2cf786ee88253626b7dca`.
This neither completes WP4 metatheory nor proves external WP3-S adequacy.

## Integrated bounded WP5 EEC-Q adapters

The first WP5 adapter candidate targets the pinned FG3 graph-only EEC-Q
fragment through a provisional module, not through the existing E7C core.
It compares exact rational state collection, total/strict graph rules and
admission failures against the canonical example evaluator. PR #47 received a
bounded first-party exact-head review at `44b0fbf25f7a73745dba4a8716ac6649d3150973`
and merged as `30d94c24abb8559fb5ef9e4c9ecb7ffad2723b97`. PR #48 adds
finite graph-tuple joint dependence, independent product, marginal, graph
union and explicit restriction; its corrected head
`5083cedffe3f1c26088165c8ff4f11df1ffbef03` passed bounded first-party
review and merged as `0b3ff687bb30a5d9ba5ad1c6b0f8efd4ff4c83e9`.
PR #50 adds source-retaining edge/phase views, explicit finite phase
identification and witnessed descent failures; first-party review at
`5cba769af2f3edce91d78b08e14717650d592a27` preceded merge
`d2a4d139f40a211d8a6fa9005a72477e8b078182`. PR #51 adds rank-one
whole-graph-state quotation and explicit inside execution; first-party review
at `53dfc8993f5302453a94c8d12189927227956f59` preceded merge
`fcb7eb6cc14f2f720790379db343e8fae16b01a5`.
PR #53 distinguishes exact source return from a retained view and a
candidate-relative phase fibre; its first-party review at
`b5c91e0a6258b0296e27692db16c09b7c6dec598` preceded merge
`482abdd2b050d621df24ff7afbfcf885d1e6e68d`.
These are provisional, selected-fragment `partial_translation` results; they
do not complete the EEC-Q adapter. The source FG3 empty joint has no arity,
while the target requires a declared arity for a zero joint. See
`E7C_0.1_WP5_EECQ_FG3_ADAPTER.md`, `E7C_0.1_WP5_EECQ_JOINT_RESTRICT.md`,
`E7C_0.1_WP5_EECQ_PHASE_VIEW.md`, `E7C_0.1_WP5_EECQ_QUOTE.md` and
`E7C_0.1_WP5_FINITE_FIBRE.md`.
The phase quotient is canonical-text-derived because FG3 has no executable
quotient/reconstruction API. The fibre is complete only relative to its named
finite candidate list; quotation admits graph payloads only. No kernel/profile/API
migration occurs.

## Gate status

First-party proposal-register verdict: `complete_with_named_deferrals`.
WP0 acceptance status: `wp0_accepted_with_named_deferrals` at PR #34 head
`d84d3fd5…`, merged as `64c49b4676…`. The deferrals are GPR-004,
GPR-010–012 and adapter/differential obligations GPR-014–018. They do not
block review of the initial foundation slice, but they remain prerequisites at
their named later gates.

## Formal and executable state

- formal vocabulary: accepted for static-semantics specification, still non-canonical;
- bounded internal AST/static rules: accepted and executable, non-canonical;
- dynamic semantics: WP3-S accepted; disposable WP3-I candidate implemented,
  unreviewed and non-canonical; denotational adequacy remains unproved;
- witness contract: accepted for bounded WP3-I; disposable emitter/checker
  candidate implemented, unreviewed and non-canonical;
- E7C IR: provisional var-only slice merged in PR #82; no full lowering adequacy,
  replayable IR witness or stable public schema/API. General runtime/API not started;
- proofs: one accepted bounded Lean feasibility spike and an integrated bounded
  Lean core extension; neither is a complete E7C proof or external adequacy result;
  theorem records otherwise contain bounded proof sketches or stated
  obligations, and T011B remains blocked on the explicit ReplayMachine-to-WP3-S
  adequacy obligation T017;
- E7C static-checker tests: first-party bounded fixtures and regression assertions;
- known E7C counterexamples: inherited catalogue plus outcome nesting and
  deterministic resource-precedence regressions; none claimed as a calculus
  theorem.

## Canonical and migration impact

None. The v0.12.1 file and existing profile/product IDs, data, schemas and APIs
are unchanged. Open PRs #23 and #31 remain open proposals, not policy. No
consumer is migrated.

## Open decisions

ADR-001–005, ADR-007–008 and ADR-011 remain open. ADR-006, ADR-009 and
ADR-010 are accepted controls, not completed metatheorems. In particular, no context discipline,
general definitional equality, universal denotational category, stable witness
format or recursion mechanism has been chosen. The accepted B1 witness contract
does not close ADR-007 or the later WP7 gate.

## Claim boundary

Internal consistency of the WP0/WP1 records is first-party review evidence only.
It does not establish source completeness beyond the stated visibility limit,
mathematical soundness, external acceptance, independent reproduction,
empirical effectiveness or product value.

## Historical S1 bounded action (superseded by the current checkpoint below)

Historical checkpoint at main `63a2e4dc4c944fb0e523b086c95dcda28c859828`;
S1 strict joint union was merged in PR #59 and finite correspondence against
the pinned, independently imported FG3 source in PR #60. That comparison
covers 1,025 finite cases and replay, including static effects, whole terminal
outcomes and ordered ledgers; it is evidence, not a general proof. PR #61 added
a Lean control skeleton proving ordered coefficients and whole failure for
abstract strict pairs under a sufficient visit budget. It does not model graph
union, rationals, cancellation, source admission, detailed ledger or Python
refinement. See `E7C_0.1_S1_LEAN_STRICT_CORE.md` and
`E7C_0.1_S1_CORRESPONDENCE_GATE.md`.

PR #62's selected FG3 candidate relates one admitted nonzero pair's
source result to its Lean S1 operational result under sufficient budget. It
models exact rational amount, fixed edge union, resource precedence, and a
single ordered ledger event; see `E7C_0.1_S1_FG3_SINGLE_REFINEMENT.md`. This
is a Lean-to-Lean relation, not a Python implementation proof.

PR #63 proves finite ordered traversal over admitted FG3 support rows,
including whole failure at the first incompatible support index and exact
visit/ledger observations at a sufficient bound. The successful row images
remain pre-collection contributions. See `E7C_0.1_S1_FG3_ROWS.md`.

PR #64 introduces a constructive internal fraction collector for exact
same-graph addition and zero removal. It proves selected cancellation and
nonzero collision cases with the original axiom allowance; see
`E7C_0.1_S1_EXACT_COLLECTION.md`. Fractions can remain unreduced and the
collector's all-list uniqueness and order-independence are not proved.

PR #65 adds a finite-list nonzero invariant for every retained exact
coefficient and states cross-product equivalence of internal fractions. It
checks reflexivity, symmetry and one alternate-denominator example; it does
not establish full fraction algebra, unique graph keys or a source bridge.

PR #66 proves graph-key uniqueness for every finite collected input list,
including preservation when an insertion collides with an existing key or
removes a zero total. Together with PR #65, every output graph occurs once
with a nonzero coefficient; no ordering or source-refinement theorem follows.

PR #67 proves transitivity of cross-product fraction equivalence for
positive denominators and commutativity of exact addition up to that
equivalence. Addition congruence and finite order independence remain open.

PR #68 proves addition congruence for either or both equivalent fraction
inputs. Equivalent unreduced coefficients therefore remain equivalent after
one exact addition; finite collection order independence is still open.

PR #69 proves exact addition associativity for three internal fractions and
shows equivalent fractions agree under the executable zero test. These
close the local algebraic prerequisites; they do not prove a finite-list
permutation theorem or Python/source correspondence.

PR #70 connects the actual finite collector's per-graph observation to an
exact coefficient fold and proves that arbitrary permutations of admitted
input rows preserve every graph's value under fraction equivalence. Output
list order and unreduced encodings can differ; no canonical codec or
source/Python correspondence follows.

The next bounded candidate supplies a selected wire bridge: Lean maps its
pre-collection `Rat` rows into the exact fraction collector, proves finite
row permutation preservation through the mapping, and round-trips the fixed
AB/AC/BC graph fragment with absent/`"marked"` tags. Python tests exercise
the actual S1 graph and fraction codec for the same finite cases. This does
not prove Python refinement, all-string tag admission, normalized wire
encoding or FG3 multi-row correspondence. See `E7C_0.1_S1_EXACT_COLLECTION.md`.

The following bounded candidate composes the finite admitted-row scan with
the internal exact collector. Compatible rows and any permutation scan
successfully under sufficient resource bounds, and the collected outputs
retain zero-free, unique-key and per-graph value-equivalence properties.
The general Lean `Rat.add` to collector addition bridge remains open:
standard normalization lemmas used in an initial proof required
`Classical.choice`, outside this gate's axiom allowance. This is not a proof
about Python's `Fraction` or full FG3 source evaluation.

The next candidate defines an edition-bound tag codec with a left-inverse
proof and extends the graph round trip beyond the single `"marked"` token.
Arbitrary admitted tag tokens and all eight fixed-edge configurations
round-trip; the concrete fixture registry maps `"marked"` and `"phase-x"` to
distinct tokens and rejects unknown names. Python's unrestricted
string admission is exercised separately and does not imply that a particular
Lean registry covers all such strings.

The next WP5 candidate starts WPC in roadmap order with a separately typed
finite allocation adapter. It differentiates semantic wholes, portion
occurrences, local states, representations and successor candidates;
differential tests cover canonical closure, coverage failures and proposal
conflicts against the v0.13 draft allocation reference model. This is a
provisional adapter, not an addition to E7C-B1's accepted grammar or proof
of general WPC conformance. See `E7C_0.1_WP5_WPC_ALLOCATION_GATE.md`.

The following bounded WP5 candidate starts MSC/0.1 with a typed finite
exact-map module. It differentiates link-wise satisfiability from global
coherence, a resource/unsupported result from incompatibility, scope-state
from observation fibres, and access equivalence from underlying state.
Its source proposal remains optional and speculative. See
`E7C_0.1_WP5_MSC_FINITE_GATE.md`.

The next selected WP5 candidate composes the experimental REC/0.1 evaluator
with its independently implemented trace checker across a closed,
versioned envelope and immutable input snapshot. The replay result retains
four information statuses, provenance groups, policy actions and hashes;
it is a bounded source integration, not E7C-B1 semantics or external truth.
See `E7C_0.1_WP5_REC_REPLAY_GATE.md`.

The following bounded candidate starts the WP5 temporal and phase adapter
using a declared finite review/approval history carrier. It distinguishes
source-retaining final views from lossy final-state projection, complete
carrier-relative temporal reconstruction from resource failure, and actual
phase crossings from two arbitrary phase differences. The reference has no
matching executable temporal model, so tests derive from the pinned source
rules rather than claim source differential reproduction. See
`E7C_0.1_WP5_TEMPORAL_HISTORY_GATE.md`.

The next selected WP5 candidate tests the informative relational
non-separability proposal as a **derived library analysis** over the existing
finite FG3 phase quotient and reconstruction fibre. It obtains separable,
non-separable and undetermined results with explicit candidate and resource
bounds; it neither establishes the full proposal nor adds a core primitive.
See `E7C_0.1_WP5_DERIVED_SEPARATION_GATE.md`.

The next WP5 candidate closes a previously skipped finite SF/CFS slice:
canonical rational point domains, exact polynomial coefficients inside
FG3 inner states, pointwise shared-parameter composition and an explicitly
different independent assignment product. Empty outer family and zero
inner state remain distinct. It is a partial translation against the
pinned first-party CG3 model, not a general symbolic-family calculus.
See `E7C_0.1_WP5_SFCFS_POINTS_GATE.md`.

The following WP5 candidate closes the finite RGP-B1 structural slice with
independent layer records, generation, typed local hosting, loss-accounted
projection, exact SR4 decoding and identity-preserving composition. It is a
partial executable differential against the pinned RGP companion; quotation,
contextual expression and general authorization remain explicit obligations.
See `E7C_0.1_WP5_RGP_FINITE_GATE.md`.

A subsequent canonical-text-derived RGP refinement adds exact whole-layer
quotation, an explicit interface-position/contract host relation and a
role/time-indexed local view while retaining source in the model. The
source companion does not export these operations; see
`E7C_0.1_WP5_RGP_HOST_QUOTE_GATE.md` for the remaining obligations.

The first WP6 provisional IR candidate lowers an admitted core variable term
into a versioned, canonically encoded typed instruction. It compares value,
terminal status, ledger and resource progress with WP3-I at finite budgets.
The source's bound success witness ID is not reproduced by this compact IR,
so full lowering adequacy remains open. See `E7_IR_0.1_VAR_GATE.md`.

## Current correction checkpoint

### WPC semantic constitution correction at main `bbf15ca03743259f52d85b1afcbc9dd9860c7fbe`

PR #114 merged the finite successor-edition SF/CFS replay. The WPC/0.2
unencoded constitution candidate under review separates semantic formation
from the prior adapter's strict all-portions payload agreement. This selected
finite rule establishes no SR tier or full WPC/0.1→/0.2 correspondence.

### SF/CFS inherited-edition replay at main `bd1d4fd48b7ca9f98802d8f9849b96297165a059`

PR #113 merged the bounded v0.13/v0.14 read-only RGP/0.2 expression
candidate, without a source fidelity theorem. The SF/CFS finite-point
successor binding under review pins X.16/X.17 and the source models at both
draft editions. General SF/CFS semantics and Lean refinement remain open.

### Finite RGP/0.2 contextual expression at main `babc31f00ed32b899a6000ae7b9bdedb084aeeb6`

PR #112 merged the RGP source-delta gate. A selected finite read-only
RGP/0.2 `Expresses` evaluator and tests are under review for the v0.13 and
v0.14 draft editions. A source differential, Lean theorem, general context
semantics and the other RGP/WPC clauses remain open. This does not replace
the RGP/0.1 adapter or close the component-completion row.

### RGP successor assessment at main `db4660f262505e81feb898909d45e757e12ff9cd`

PR #111 merged an instance-level source/IR transcript checker and conditional
Lean paired-path theorem; the all-input actual Python/IR implementation proof
is still open. The new RGP/0.1→/0.2 successor gate under review identifies
changed and text-identical X.18 subsections and states a separately typed
contextual-expression judgement. Its versioned executable evaluator and
semantic adequacy theorem are not yet built. This is not full RGP coverage.

### Precision at main `0e84e7b501076534fc42ff8dcccb58b1311cdee3`

PR #110 merged the edition completion gate and source-text audit. Its test
file contains four tests: one pinned-edition audit and three mutation checks.
The completion vocabulary distinguishes coverage with an explicit open link
from a complete semantic correspondence; the former cannot close a row.
The selected Joint implementation-instance gate under review compares both
actual-path transcripts and full observations with the staged rule and adds
a conditional paired-path Lean theorem. The universal Python/IR-to-Lean
implementation link remains unproved.

### Edition completion assessment at main `08c6d7b8515313ff2f6614c698f51a41fe2bb42e`

PR #109 reconciled the terminal gate and ledger. The new
`E7C_0.1_EDITION_COMPLETION_GATE.md` records separate fidelity, executable,
counterexample and proof obligations for EEC-Q, SF, CFS, RGP, WPC, MSC and
REC at their declared editions. Inherited changes in v0.13 and v0.14 and
optional WPC-Evolution and WPC-Distributed are accounted for separately.
The pinned textual audit does not prove semantic or implementation fidelity.
WPC-Distributed remains unsupported, and E7C is not complete.

### Reconciliation at main `70ca06a9d599c0c00a020ac367088242f2e62087`

PR #108 merged as `70ca06a9d599c0c00a020ac367088242f2e62087` after
exact-head review of `8eebfa1623f2fdee6762275f5c9296c58b8d3ace`.
Both selected outer source and IR execution paths now emit a copied terminal
transition at their actual result site. The separate checker requires one
final terminal carrying exact outcome, progress, stage and `secondStarted`,
and rejects missing, duplicate and forged records. The site audit checks
the result helper contains its terminal probe. All seven workflows passed,
including Lean build/axiom audit and source/IR replay. This is bounded
transcript evidence, not a proof of all-input Python control-flow semantics.
The remaining implementation refinement obligation and the published
v0.12.1/RGP2 and product boundaries are unchanged.


### Reconciliation at main `ada63dff19e284af08d21f51d04fc687d07300d5`

PR #107 merged as `ada63dff19e284af08d21f51d04fc687d07300d5` after
exact-head review of `3e45f3ff26e67d6f8059f18328dd18e2f465754e`.
It statically checks the four selected Python functions for adjacent probes
after every direct charge and ledger append, correct second-attempt start
ordering, and sink forwarding to first-stage children. Its adversarial tests
remove or reorder probes. All seven triggered workflows passed. This is
pinned-code syntactic coverage only; it does not establish interpreter
semantics or the all-input Lean refinement premise. The next selected
package records the outer terminal outcome at the actual return site so a
transition transcript includes its terminal constructor as well as charges
and appends.


### Reconciliation at main `1bd08763545a081f2604cb862abf38fe35a2a9ec`

PR #106 merged as `1bd08763545a081f2604cb862abf38fe35a2a9ec` after
exact-head review of `5e430969d57894aac9cf7d6e88ec4121a67383c8`.
The selected source and independent IR execution functions now emit opt-in
copies of their actual charge and ledger-append transitions. A separate
checker rejects omitted charges, ghost rows, changed event metadata and
failed second-attempt append with missing or false `secondStarted`. Lean
proves that any source- or IR-labelled transition certificate satisfying
the formal constructors yields the staged evaluator's observation, exact
correlated ledger prefix and resource-only exhaustion. All seven triggered
workflows, including Lean build/axiom audit and source/IR replay, passed.
These are conditional all-list theorems and finite execution evidence;
instrumentation completeness, Python control-flow semantics, child-result
binding and all-admitted-input correspondence remain open. The next bounded
candidate statically audits charge/append instrumentation sites, but that
audit also cannot prove Python semantics. Published v0.12.1/RGP2 and product
APIs remain unchanged.


### Reconciliation at main `047b59d2d1e04f67a8b6510c45edb9a393fac1b3`

PR #104 merged as `0b1e292c59008cb7d0260ef85f3ae44f1df4d34f` with
the post-#103 state ledger. PR #105 merged as
`047b59d2d1e04f67a8b6510c45edb9a393fac1b3` after exact-head review of
`f0da4184b4f7a4569a751ed9392e4969dcc74632`; Lean build/axiom audit and
source/IR replay passed. Its selected FG3 correlated Joint encoding retains
canonical edge order, nullable canonically UTF-8 encodable string tags,
reduced signed rational coefficients, unique support and row order. The
inverse rejects constructed unpaired-surrogate tags. The event mapper
validates effects and predicate editions before projecting those metadata
fields out of Lean `Event`. Lean proves the exact value/partition and abstract
wire-event/budgeted-ledger relations, while finite tests compare Python and
independent IR observations including the failed second append. This is a
provisional encoding and model-simulation bridge. An all-admitted-input
refinement theorem over the actual Python and IR execution paths remains
open. The published v0.12.1/RGP2 reference and product APIs do not change.


### Reconciliation at main `a19e4fd13845463d15add2a0b4b76bf9257dec65`

PR #102 merged as `fb51c34a33ae2cbbbfb73663b8b3e1218541cbb9`.
Its all-list theorem partitions complete correlated graph-pair rows, preserving
exact rational coefficients. It contains no resource claim. PR #103 merged as
`a19e4fd13845463d15add2a0b4b76bf9257dec65` after exact-head review of
`f1eb3f3ad3eafd62959b0b66d8e5029b51a651fa`. The staged Lean evaluator
charges a step, marks the second attempt started when applicable, checks
ledger capacity, and only then visits the next row. Its independent complete
plan includes every first-stage retained row, even if the second predicate
later excludes it. Lean proves all-input sufficient-budget equality to #102's
three-way partition and, for every short budget and policy, resource-only
termination with the exact plan prefix, charged steps and stage-specific
progress. The explicit failed second-attempt append boundary and finite
Python source/independent IR replay both pass. Lean build and axiom audit
passed at the reviewed head. This accepts the selected internal operational
slice, not the unproved admission/encoding and all-input Python/IR-to-Lean
refinement bridge, general EEC-Q adequacy, profile adoption or product
benefit. Published experimental v0.12.1/RGP2 and B1/B2/B3 remain unchanged.

### Historical reconciliation at main `7cc35af720ca0897e4d9c303a31e6840bb48d9bc`

### Reconciliation at main `7cc35af720ca0897e4d9c303a31e6840bb48d9bc`

PR #100 merged at `71a85828a241b08fefbefbe2b53b4ea01a9c61f6`.
Its exact merged diff and adversarial fixtures received a bounded first-party
review recorded in `E7C_0.1_WP5_EECQ_JOINT_IR_GATE.md`; the correlated Joint
restriction is no longer a pending review candidate. PR #101 merged at
`7cc35af720ca0897e4d9c303a31e6840bb48d9bc`. Its separately versioned
two-stage operation restricts the retained correlated Joint a second time,
preserves first and second exclusions and exact rational rows, and compares
source replay to independent IR execution over selected finite cases. Its
ten Lean cases check abstract success-only control after value erasure.
Hosted checks passed at PR #101 exact head
`690892429a8f098c8f6453b43a2e64e698817d86`; this does not establish
all-input Python/IR correspondence, general EEC-Q adequacy or independent
review. The next gate is a rational-preserving all-input conservation and
source-to-IR observation relation under explicit admission and resource
premises. The B1/B2/B3 and published v0.12.1/RGP2 editions stay unchanged.

### Reconciliation at main `124a8e573cb1490ce0d9a16238de20a5ce28a179`

The checkpoint below is historical, not a description of today's main.
PRs #90–#97 established the owner-authorized, opt-in B2 success-only
sequence, bounded source/IR replay, finite cross-edition cases and abstract
Lean trace properties. PR #98 merged as
`b05303b683e54d6569330e57428d076776582776`: separately versioned B3
admits a strict `Sigma-A → Sigma-B` first map and a total identity on
`Sigma-B`; its complete success and first-domain-error packages replay
independently. PR #99 merged as
`124a8e573cb1490ce0d9a16238de20a5ce28a179`: typed B3 IR independently
executes this finite slice and compares the entire terminal, ledger and
resource progress against source replay. The research suite passed 180 tests
at the #99 head and hosted B3/IR checks passed. The E7C B1 grammar, B2
edition and published v0.12.1/RGP2 source were not migrated. These results
do not prove general sequencing, compiler adequacy, EEC-Q profile fidelity,
external reproduction or product benefit.

The then-selected WP5/IR review candidate added an edition-bound correlated
Joint restriction that retains both kept and excluded exact-rational joint
rows, with a whole-input replay and independent rational enumeration. It is
separate from the existing state-only `restrict_absent` adapter; see
`E7C_0.1_WP5_EECQ_JOINT_IR_GATE.md`. Its subsequent merge and bounded review
are recorded above; this paragraph describes the historical #99 checkpoint.

At former main `fd2eb1558042ec8173cc7711150126239e1f6adc`, PRs #79–#82
were merged and internally tested while PRs #81 and #82 had the re-bound
admission defects. PR #83 at exact head
`73aadd50bd21c3425f728fcd400e8094b86b9d92`, merged as
`0c3d54312982c318a5df5a601bd554365365d855`, adds one strict layer
reconstructor reused by quotation and SR4, and typed IR binding admission
against the minimal pinned atomic carriers. Rebound-ID and full-key fixtures,
148 research tests, 72 adapter tests, 12 source RGP tests and 11 hosted
workflows passed. This is internal correction evidence; neither the
underlying profiles nor the IR are independently reproduced or promoted.

The former next build-map gate was delivered in PR #85 (merged as
`2a31ec2a810d1cee8a0c8f0300b56d6250b54cd0`): the versioned WP5/IR
matrix and a finite graph variable with complete replay and a conventional
baseline. PR #86 (merged as
`0a5267ab3bd83cb33b8aa5d650eefe3572a4f81b`) added a separately
versioned strict named-map IR with inline source replay and a two-step
independent executor. Both are bounded internal results; complete WP6
lowering adequacy, WP7, external profile fidelity and product benefit remain
open. PR #87 subsequently added a total map with explicit typed output and
comparative finite cases. No source-truth or two-consumer adoption inference
is made.

At PR #87 merge `6fda83b3913b1d87022f556ad8f771949d5e7bcf`, the
versioned named-map IR has two selected direct-variable maps, an explicitly
typed output, complete replay and finite differentials. A proposed two-map
chain failed source static typing because map application returns an Outcome.
PR #88 therefore added a separately versioned direct-variable
source-preserving/lossy view IR slice and recorded that rejected chain as a
negative fixture. Composition requires an explicit outcome-elimination design;
neither this branch nor the earlier IR editions change that source rule.

PR #88 merged as `1518da87da76b6dff2ba3592152e9ba27f2f9342`, retaining
the source/view versus lossy projection distinction in a versioned direct
variable IR. PR #89 pins five complete replay fixtures in a cross-slice WP6
manifest with static effect/ledger checks. These
finite internal cases do not close WP6 lowering adequacy, WP7 independent
acceptance, WP8 empirical pilot, WP9 external reproduction or WP10 promotion.

The owner authorized an explicit success-only sequencing research rule in a
new provisional edition. PR #90 merged the decision record as
`5ecd397a07c8ca940d803cdb03f570b6dd7a4530`. PR #91 merged as
`a1f253e1dd4841adff3c8442a1629e1cf704708e`, implementing one B2 named-map continuation,
an independently replaying checker and a versioned IR differential. The
exact review head is recorded in its PR. A cross-edition manifest pins B1's
five selected observations and six B2 terminal paths, including early stops
and both resource limits. B1 remains terminal-only; this
bounded successor does not close the abstract ADRs or general WP6/WP7.
The next provisional B2 rules record gives the selected static inference,
bounded charge order, short circuit and model-relative whole observation;
B2-T01–T06 remain proof obligations. A zero-step adversarial witness case
requires no child derivation and rejects an inserted child after re-digesting.
The following abstract Lean trace module checks failure short circuit and
exact prefix/payload retention within its own definition; the source bridge,
resource/ledger admission and B2-T05 in full remain open.
The abstract success-branch lemmas separately track the continuation's
ordered ledger suffix and entry step, and the pre-continuation resource limit;
the Python/Lean bridge and general B2 proof obligations remain open.
A finite bridge candidate relates 13 selected replayed B2 and IR runs to
independently replayed B1 direct continuations and generated Lean trace
examples. Exact nominal JSON is compared before a documented Nat/ledger
erasure; the general Python/Lean refinement remains unproved. See
`E7C_B2_FINITE_LEAN_BRIDGE.md`.
The following abstract Lean candidate proves effect membership under separate
child and continuation effect premises across every abstract terminal path.
The actual source effect/ledger theorem remains open pending the interpreter
bridge and admission proof.

The earlier S1 proof strand remains open: instantiate and validate a pinned Python/Lean tag registry and establish
the precise Python/Lean coefficient relation,
then relate the admitted pre-collection FG3 rows and collector to canonical
S1/FG3 support and pinned Python codecs. Prove soundness and bounded completeness
under exact type/edition/resource premises or record a counterexample. Review the remaining WP5 fidelity boundary and extend the bounded WPC
module with versioned E7C types and rules. Preserve the
distinction between a view, lossy identification and candidate-relative
fibre; finite fixtures establish no general EEC-Q fidelity.

Lean remains provisional; compare Rocq and Isabelle where practical before
permanent proof-assistant selection. Do not promote Lean evaluator equality or
the S1 skeleton into an external WP3-S adequacy or correctness claim.
