# E7C MSC finite Boolean obstruction proof gate

Status: `merged_bounded_fixture; finite_fixture_only; no_general_semantic_fidelity`
at PR #116 merge `aa4b549be9711d6c7341d7176cdfd1a1b69d3fbc`.

The proposal fixture `proposals/msc-0.1/fixtures/global_obstruction.json`
has SHA-256 `b1e87b46a57b5c8a52a5c41737dbf96250637e5f49b02854bbe33303c8bd3d2a`.
`test_msc_lean_obstruction.py` pins its edition, diagram, bounded budget, ordered
carriers/scopes/maps/links, complete two-valued tables and exact criterion.
It checks the pinned first-party evaluator and independent finite adapter both
scan eight combinations, report each link satisfiable, and find no compatible
family. Changed flip or criterion cannot inherit the proof by fixture identity.

`E7CMSCObstruction.lean` formalises precisely this Boolean case. Its equations
are `s = r`, `t = s`, and `¬t = r`; it proves each link has a solution and no
single assignment satisfies all three. The source string values `"0"` and
`"1"` map to `false` and `true`, respectively; the fixture encoding check
verifies the map tables and link bindings for this interpretation. The Lean
theorem makes no use of the Python execution result. The differential tests
provide finite execution evidence, not a proof of Python implementation
correctness. The source evaluator and adapter remain distinct paths.

Open obligations: a typed MSC rule and denotation for arbitrary admitted
diagrams; proof that the source and independent adapter implement that rule
under admission and resource premises; MSC derivability from the existing
calculus or a justified primitive, plus conservativity; general failure,
resource, access and arbitrary criterion preservation. Pairwise consistency
is not a substitute for global consistency, and resource exhaustion is not
evidence for a negative result. This row is **covered with an open link**, not
**complete**, pending those obligations. MSC remains a
research proposal, outside the v0.14 canonical addition list.
