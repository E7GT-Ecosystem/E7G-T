# Selected terminal transition in source/IR transcripts (merged provisional)

Accepted bounded head: PR #108 `8eebfa1623f2fdee6762275f5c9296c58b8d3ace`,
merged as `70ca06a9d599c0c00a020ac367088242f2e62087`.
Baseline was main `ada63dff19e284af08d21f51d04fc687d07300d5`, the merged
provisional transition-site audit. The #106 Lean `Trace.terminal` constructor
requires a completed typed outcome from the current cursor.

Both actual outer execution paths now emit a copied terminal record at their
result site. It contains terminal outcome and exact progress, completed steps,
ledger count, stage and `second_started`. The independent transcript checker
requires exactly one terminal as the last record and rejects a missing,
duplicated or forged terminal. The selected site audit verifies the outer
result helper contains that probe. The source's replay witness and the IR's
ordinary return shape remain unchanged. Child calls still emit only charge
and append records; their result is reflected in the outer terminal record.

This closes a concrete *observable transcript* omission. It does not prove
that Python terminal control flow, first-stage child results, JSON parsing,
or emitted states instantiate Lean's `finished` relation for every admitted
document. The all-input source/IR-to-Lean implementation proof remains open.
