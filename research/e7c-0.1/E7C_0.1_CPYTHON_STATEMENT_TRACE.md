# CPython Joint statement-trace increment: serializer and final guard

This is a separate draft continuing the extensional RawJson equality counterexample repair in #148. Its base is the #148 branch at the exact parent recorded by this pull request. #146 remains a draft at its historical accepted bounded head; #147 remains draft evidence of the ordered-object mismatch; #148 remains a draft that repairs the model equality relation.

## Pinned source sites

The existing source audit pins normalized ASTs (SHA-256) for the selected entry point and helper bodies:

| File, qualified function | AST SHA-256 |
|---|---|
| `e7c_eecq_joint_restrict_b1.py:admit` | `02686f71546ef8d328c32e5321a7eec0d08628b50281cdbc7ebc4b4ee9900408` |
| `e7c_eecq_joint_restrict_b1.py:_graph` | `e24060d0457a10f3cf155f49966203d9eb1a4e658417fc739f243d78daa1d295` |
| `e7c_eecq_joint_restrict_b1.py:_row` | `93d2971bdcf3796b7e688cd1ccff45a3d4ed7934b9b7ef9d1c22cdd7b28d386f` |
| `e7c_eecq_joint_restrict_b1.py:rows` | `d398cd44f71be111bfb0598805813a89d445ad09c2eb6fe3b2c3e535a8ab68a0` |
| `adapters/eec_q_fg3_joint_b1.py:Joint.__post_init__` | `bf054e3ee0a2271a49a122b1350a54afc8a9f77685f81796cc93fe19e0778b27` |

These are exact syntactic pins already enforced by `e7c_joint_first_helper_sites.py`; they identify source bodies but do not themselves prove CPython semantics.

## Increment boundary

`E7CJointCPythonNormalReturnTrace.lean` formalizes the row-serialization suffix as field-read/write events and a final equality-guard branch. It proves that those recorded operations yield the model's encoding of the returned ordered rows. The equality branch, under the operation-level equality contract, gives recursive extensional equality with the original raw rows. Composing with #146's typed normal-execution model derives the canonical raw-row consequence.

This remains a bounded suffix increment, not the requested end-to-end statement-level normal-return theorem. In particular, it consumes #146's `ModeledNormalExecution`, whose trace already represents parsed typed input, Joint-normalized result, and a successful modeled rows guard. It does not derive that trace from sequential execution of `admit`, nor does it derive raw admission, exact type checks, Config/Fraction creation, or Joint normalization from the pinned Python statements. Those are explicit gaps for the next increment.

## Operation-level premises consumed

| Premise | Use | Status |
|---|---|---|
| Pinned ASTs above identify the intended helper bodies | Source identity boundary | Focused source-test evidence; AST identity is not semantic adequacy |
| Graph field reads return the selected object's `edges` and `tag`; Fraction field reads return `num` and `den` | Serializer field trace | Individual host-operation premise |
| Dictionary writes preserve their key/value associations; list construction preserves the explicit row sequence | Derive model output from row events | Individual host-operation premise; CPython adequacy remains open |
| Python JSON equality is recursive, object-key extensional equality on unique-key objects, with ordered arrays and exact scalar distinctions | Final guard branch to `rawJsonEquivalent` | Explicit CPython host-operation premise |
| The typed result path is a `ModeledNormalExecution` and reaches the normal-return branch | Compose with #146's exact-row theorem | Model premise, not derived from pinned statements here |
| The pinned process completes these operations without exception, nontermination, timeout, or external resource interruption | Interpretation as a normal execution | Separate host/resource premise |

No premise supplies a completed serialized row array or a precomputed equality boolean to the trace theorem. The theorem does, however, consume the already modeled typed normal execution from #146; consequently it cannot be cited as proving the full admission-to-return path.

## Edge and adversarial distinctions

The composed row encoder is ordered and retains both Joint coordinates, edge sequence, nullable tag, and the exact rational numerator and denominator of the returned value. Together with #148's focused fixtures, the intended distinctions are: reordered object keys compare equal; row arrays and Joint coordinates remain ordered; edge arrays remain ordered; null differs from empty string; duplicate/cancelled rows are constrained by the normalizer/guard path; and raw 2/4 differs from the canonical reduced 1/2 encoding. These model facts and regression fixtures do not replace the open CPython adequacy premises.

Invalid admission, helper exceptions, nontermination and resource interruption remain separate outcomes. This suffix proves nothing about their production by a running host.

## Validation

The workflow runs the existing pinned helper-AST, translation and edition-source checks, the focused sort/constructor and raw-admission fixtures, the aggregate Lean build, and the dedicated normal-return-trace and existing namespace axiom audits. The exact head and run result are added below after hosted validation.

## Remaining obligations

1. Derive JSON-object admission and exact key/type checks from statement-level execution.
2. Derive Config and Fraction creation, Joint normalization, including rejection/copying behavior, from the pinned operation semantics.
3. Compose those steps with this serializer and final guard, retaining distinct invalid, exception, nontermination and resource outcomes.
4. Close the required CPython operation adequacy links or leave each precisely open.

Only afterward proceed to the independent IR link under both package bounds and the stage-two failed append. EEC-Q and E7C remain incomplete.
