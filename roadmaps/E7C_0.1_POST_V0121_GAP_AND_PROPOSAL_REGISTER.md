---
title: "Post-v0.12.1 Gap and Proposal Register"
version: "0.1-seed"
date: "2026-09-15"
status: "WP0 prerequisite; non-canonical research control"
repository: "E7GT-Ecosystem/E7G-T"
baseline: "E7G-T v0.12.1-experimental-RGP2"
roadmap: "E7C/0.1"
---

# Post-v0.12.1 gap and proposal register

**Edition note (2026-09-25):** This register remains an edition-pinned record
of pre-v0.15 proposals. The new v0.15/MSC1 canonical source includes the
selected WPC, REC and MSC definitions but does not erase the recorded
conformance, proof or product migration gaps.

## 1. Purpose

This register prevents E7C/0.1 from formalising an incomplete snapshot of
v0.12.1. It records gaps identified after v0.12.1, the proposals intended to
address them, their semantic effect, evidence, compatibility risk and proposed
destination.

This file is a **seed inventory**, not a promotion decision and not a claim of
completeness. Neither inclusion nor an existing implementation makes a proposal
canonical, mathematically sound, product-ready or part of E7C.

## 2. Authority and invariants

- v0.12.1/RGP2 remains the published experimental canonical baseline.
- No entry in this register edits, supersedes or silently reinterprets v0.12.1.
- Later drafts and profiles remain at their recorded maturity.
- E7C must evaluate proposals; it must not assume that every later construct is
  a core primitive.
- Product implementations are evidence and regression material, not authority
  over the calculus.
- A proposal may be admitted only at an explicit destination:
  `e7c_core`, `derived_e7c_library`, `profile_adapter`,
  `experimental_profile`, `product_local`, `defer` or `reject`.
- Unknown compatibility stays `unknown`; it must not be rounded up to
  conservative compatibility.

## 3. Successor-lineage coverage

The machine-readable companion
`roadmaps/E7C_0.1_SUCCESSOR_SOURCE_COVERAGE.yaml` pins the presently visible
successor lineage. It includes:

- the complete v0.13/WPC draft and release manifest;
- the standalone WPC/0.2 package, including evolution and distributed
  subprofiles where declared;
- the complete v0.14/REC draft and release manifest;
- the standalone REC/0.1 package;
- merged MSC/0.1;
- open REC-EVAL/0.1;
- open relational non-separability;
- E7C/0.1 itself and its review-derived corrections.

Coverage is not limited by version naming. Any non-canonical addition,
suggestion, counterexample or alternate draft after v0.12.1 is in scope even if
it is labelled v0.12.x, has no version, predates v0.13 publication, lives only
in an issue/PR discussion, or was rejected or superseded.

The manifest is a rolling inventory. At WP0 closure it must be regenerated
against an exact repository commit and timestamp, all issues and PRs through
that cutoff must be searched, and each included artifact must map to one or more
register entries. An omission cannot be interpreted as rejection.

## 4. Required entry fields

Every entry must record:

| Field | Required meaning |
|---|---|
| ID | Stable register identifier |
| Gap | What v0.12.1 cannot currently express, distinguish or establish |
| Proposal | Construction or correction offered |
| Semantic effect | Clarification, correction, conservative extension, new mathematics, conjecture or product mechanism |
| Dependencies | Exact baseline constructs and source pins |
| Compatibility | Conservative, breaking, mixed or unknown, with rationale |
| Evidence | Example, proof, counterexample, bounded model, implementation or empirical result |
| Proposed destination | One destination from the controlled vocabulary |
| Decision status | Discovered, triaged, admitted for testing, validated, rejected or deferred |
| Open obligations | What must be resolved before disposition |

## 5. Initial proposal inventory

| ID | Gap in or beyond v0.12.1 | Proposal and semantic effect | Compatibility | Current evidence | Proposed destination | Status / open obligations |
|---|---|---|---|---|---|---|
| GPR-001 | A generated whole and its local parts/views do not yet have a complete reciprocal constitution contract. | WPC/0.2 distinguishes semantic whole, constitutive presentation and constituent occurrence; adds coverage, constitution, materialisation, reconstruction and reciprocal closure. New mathematics, not a wording clarification. | Unknown. It is intended to extend RGP, but identity, normalization, failure and loss reflection must be tested against v0.12.1. | v0.13 draft plus bounded WPC/0.2 model and tests. | `experimental_profile` initially; candidate `profile_adapter`. | Admitted for testing. Determine whether any nucleus concepts are truly profile-neutral. |
| GPR-002 | v0.12.1 does not provide a governed constructive calculus for claim/evidence status, inference, modality, time, decision and abstention. | REC/0.1 adds a four-status non-cancelling reasoning–evidence calculus and replayable witnesses. New applied calculus. | Mixed/unknown. It must preserve evidence dependence, provenance, scope, authority, time, model edition and decision policy without redefining truth. | v0.14 draft; REC/0.1 package; open REC-EVAL/0.1 PR #31 supplies a proposed empirical method only. | `experimental_profile` and later `profile_adapter`; not presumed core. | Admitted for testing. Requires adapter conservativity and independent empirical evaluation. |
| GPR-003 | Independently constituted wholes at several scopes lack an explicit global compatibility and obstruction analysis. | MSC/0.1 adds typed finite scope diagrams, common-codomain comparison, global compatible families, access quotients and pairwise/global obstruction. New mathematics. | Unknown. It may compose with RGP/WPC but must not be conflated with recursion or promoted by finite enumeration alone. | Merged proposal and bounded exhaustive MSC-B1 model, including a global obstruction fixture. | `experimental_profile`; candidate `derived_e7c_library` after proof review. | Admitted for testing. General composition and unbounded claims remain open. |
| GPR-004 | “Each part expresses the whole” lacks an inquiry-relative test that distinguishes reconstructible relations from invented hidden connections. | Relational non-separability defines separable, non-separable and undetermined relative to source, views, inquiry and equivalence. Clarification plus candidate derived predicate. | Expected conservative, but not established while PR #23 remains open. | Informative note and examples on PR #23; no canonical or runtime change. | `derived_e7c_library` if derivable; otherwise `defer`. | Discovered. Must first attempt derivation from projection/reconstruction and loss semantics. |
| GPR-005 | v0.12.1 has profile-specific operations but no shared typed/effectful account of partiality, loss, authority, evidence, history and resources. | E7C/0.1 proposes typed judgements, tagged outcomes, explicit effects and witnessed evaluation. Candidate foundation architecture. | Unknown by design. Conservativity must be demonstrated per source profile and per stated equality/observation criterion. | Foundation mandate and formal-review corrections on PR #33; no implementation yet. | `e7c_core` only for the minimal profile-neutral nucleus; other material must be derived or adapted. | Admitted for specification, not implementation. WP0–WP4 gates apply. |
| GPR-006 | Existing source profiles use several inequivalent notions of identity, sameness, phase and observation. | Separate definitional, denotational, observational, phase, isomorphism and canonical-representation relations. Correction to the foundation plan. | Intended conservative only if source distinctions are reflected and no new equations are introduced. | PR #33 review correction; proofs absent. | `e7c_core` relation framework plus profile-specific criteria. | Admitted for specification. Decidability and congruence obligations remain open. |
| GPR-007 | Successful-looking executions could omit declared loss/effects, and failures could erase their causal ledger. | Effect preservation/runtime-ledger soundness, including non-success outcomes and composition. Correction to the foundation plan. | Expected conservative for explicit records; adapter impact unknown. | PR #33 review correction; proofs absent. | `e7c_core`. | Admitted for specification. Formal ordering/composition of effects remains open. |
| GPR-008 | Projection/reconstruction soundness alone permits vacuous empty success and does not justify phase classification. | Require complete finite fibres for bounded success; otherwise return `undetermined` or `resource_limit`. Correction/clarification. | Likely conservative for v0.12.1 intent, but executable profiles need differential tests. | PR #33 review correction and existing bounded reconstruction examples. | `e7c_core` bounded rule plus adapter obligations. | Admitted for specification. Abstract/unbounded completeness remains separate. |
| GPR-009 | A shared IR or runtime introduced too early could encode product-specific meanings and make later mathematical correction breaking. | Defer E7-IR/compiler/runtime until static, dynamic, denotational, witness and vertical-slice reviews pass. Governance correction. | Conservative process constraint. | E7C mandate stop conditions. | `defer` until WP6 gate. | Validated as current build constraint; not a mathematical result. |
| GPR-010 | REC/0.1 has no independent empirical evidence that its additional structure improves reasoning or assurance relative to matched baselines. | REC-EVAL/0.1 supplies a preregistered comparison method, frozen case/run contracts, blinded adjudication and scoring. Empirical-method proposal; it does not alter REC or E7C semantics. | Not a semantic extension. Methodological compatibility remains unknown until its protocol and metrics are reviewed. | Open PR #31; synthetic fixtures validate the harness only and are not performance evidence. | `defer` as an empirical gate for Pilot A; never `e7c_core` or `profile_adapter`. | Discovered. Requires methodological review, merge decision and independent real runs before any effectiveness claim. |
| GPR-011 | WPC Core does not define how an admitted whole changes itself through proposals, candidate successors, commitment and representation refresh. | WPC-Evolution/0.1 adds optional endogenous evolution. New optional profile mathematics, independently selectable from WPC Core. | Unknown. It may change identity, history and authority behaviour and therefore requires separate laws and adapter tests. | v0.13 draft; WPC/0.2 conformance records only partial bounded support. | `experimental_profile`; candidate later `profile_adapter`. | Admitted for testing, independently of GPR-001. Commitment authority, stale-representation handling and history laws remain open. |
| GPR-012 | WPC Core does not establish distributed constitution, consensus, transport, availability or fault semantics. | WPC-Distributed/0.1 sketches optional distributed operation without making infrastructure guarantees. New optional/profile-specific mechanism. | Unknown and potentially product-dependent. Structural compatibility cannot establish distributed-systems correctness. | v0.13 draft; WPC/0.2 conformance matrix records this capability as unsupported. | `defer` or `product_local` pending a separate distributed-systems contract; not initial E7C core. | Deferred. Requires fault, trust, authority, consistency, transport and security models plus independent implementation evidence. |
| GPR-013 | The revision label `RGP2` can be mistaken for profile identity `RGP/0.2`, obscuring the actual canonical source. | Preserve `RGP/0.1` as the profile identity declared by v0.12.1, revision RGP2. Baseline identity correction; not new mathematics. | Retain exactly. No successor text may be substituted for this canonical source. | v0.12.1 experimental canonical reference and bounded RGP model from PR #28, with their exact stated scope. | `profile_adapter` sourced from canonical `RGP/0.1`; candidate derived recursive libraries only after proof. | Admitted for preservation review. Pin profile identity and revision separately. |
| GPR-014 | v0.12.1 does not contain the later whole–portion coherence bridge introduced with the v0.13 successor line. | Treat `WPC/0.1` as a v0.13 successor addition and review WPC/0.2 as a further extension rather than a silent replacement. Successor mathematics and lineage clarification. | Unknown until the RGP/0.2–WPC/0.1 introduction and WPC/0.1–WPC/0.2 extension differentials are reviewed. | v0.13 draft and release manifest. It is not evidence about canonical v0.12.1 content. | `experimental_profile` initially; possible `profile_adapter` with an explicit extension relation to GPR-001. | Admitted for testing. Requires laws, failures, migration effects and counterexamples for non-equivalent WPC/0.1 and WPC/0.2 cases. |
| GPR-015 | A self-contained successor draft can silently alter inherited EEC-Q syntax, rational coefficients, canonical identity, domains or cancellation behaviour. | Review EEC-Q preservation independently in every successor snapshot. Preservation control, not a new algebra. | Retain is intended; exact source/draft differential required. | v0.12.1 canonical EEC-Q material and successor release manifests. | `profile_adapter` from the canonical source; successor copies are comparison targets. | Admitted for preservation review. No coefficient reinterpretation as probability, evidence weight or quantum amplitude. |
| GPR-016 | A successor draft can silently collapse SF dependence, family identity, view, restriction or realisation order while claiming inheritance. | Review SF preservation independently in every successor snapshot. Preservation control. | Retain is intended; exact source/draft differential required. | v0.12.1 canonical SF material and successor release manifests. | `profile_adapter` with possible generic family library only after conservation proof. | Admitted for preservation review. Preserve dependence and ordered realisation outcomes. |
| GPR-017 | A successor draft can silently reverse or flatten CFS family-of-state nesting or change pointwise/pushforward semantics. | Review CFS preservation independently in every successor snapshot. Preservation control. | Retain is intended; exact source/draft differential required. | v0.12.1 canonical CFS material and successor release manifests. | `profile_adapter` or derived composition preserving nesting direction. | Admitted for preservation review. Require positive and reversed-nesting counterexamples. |
| GPR-018 | v0.13 changes recursive generative–projective material beyond canonical RGP/0.1 without an explicit foundation migration differential. | Treat `RGP/0.2` as a v0.13 successor change and compare it against canonical `RGP/0.1`, including generation, local differentiation, projection, reconstruction, source retention, regimes, time and recursion. New successor revision. | Unknown; the v0.13 manifest's `adapt` label is a claim to be verified, not proof. | v0.13 draft/release manifest and bounded RGP evidence, separated by edition. | `experimental_profile` and candidate `profile_adapter` only after the differential is accepted. | Admitted for testing. Record preserved rules, additions, changed outcomes and ID/data/API implications. |

## 6. Missing-source capture

WP0 must actively search for and register:

1. originating-author suggestions made after the v0.12.1 publication;
2. superseded or alternate v0.12.2/v0.13 drafts, review sheets and
   counterexamples, whether or not merged;
3. issues, PR discussions and product discoveries that expose a kernel gap;
4. product-local constructs that may indicate a reusable abstraction;
5. rejected proposals, so they cannot silently return under new names.

For every discovered item, WP0 must pin a commit, file digest, issue/PR URL or
other immutable source reference. If a source cannot be recovered, record the
missing source and its known claim; do not reconstruct it from memory and call
that reconstruction authoritative.

## 7. Admission and closure rules

A proposal is ready for foundation use only when:

1. its gap is demonstrated by a concrete example or counterexample;
2. its semantics and non-claims are stated;
3. its dependencies and exact source pins are recorded;
4. compatibility with v0.12.1 is classified with evidence;
5. affected equality, failure, loss, identity, scope, authority and temporal
   distinctions are identified;
6. its destination is reviewed;
7. required laws, witnesses and counterexamples are named;
8. any bounded evidence is labelled bounded;
9. product evidence is not substituted for mathematical validation.

WP0 cannot close while a known post-v0.12.1 proposal lacks an entry or while a
material entry has no reviewed destination. WP1 may draft neutral vocabulary in
parallel, but no E7C vertical-slice implementation may begin until this register
has a recorded review verdict of either:

- `complete_for_initial_foundation_slice`; or
- `complete_with_named_deferrals`.

## 8. Product-development hold

Until the initial-foundation-slice verdict:

- preserve current tested product states;
- allow maintenance, security repairs and corrections that do not introduce
  new shared semantics;
- do not begin Q-A3, activate new E2CI semantic profiles, extract a shared
  runtime, stabilise E7-IR, or migrate product identities;
- treat product implementations as differential fixtures for later adapters.

Exceptions require an explicit coordinator decision naming the bounded work,
why it cannot wait, and how it avoids pre-empting the foundation.

## 9. Decision record

Current register verdict: `incomplete_seed_inventory`.

This verdict authorises gap discovery, classification, counterexample work and
WP0 documentation only. It does not authorise the E7C implementation slice,
kernel promotion or product migration.
